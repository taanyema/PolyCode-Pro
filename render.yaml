services:
- type: web
  name: polycode-pro
  runtime: python
  buildCommand: "pip install -r requirements.txt"
  startCommand: "python app.py"
  envVars:
    - key: PORT
      value: 5000
  # On installe juste le moteur Octave
  apt_packages:
    - octave